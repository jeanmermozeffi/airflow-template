# =============================================================================
# requirements.txt — Environnement de développement local (IDE, tests)
#
# Pour Docker : l'image apache/airflow:<version> embarque déjà apache-airflow.
# =============================================================================

# ── Apache Airflow + providers ────────────────────────────────────────────────
apache-airflow==3.2.1
apache-airflow-providers-postgres>=6.2.0
apache-airflow-providers-mysql>=6.3.0
apache-airflow-providers-smtp>=2.2.0
apache-airflow-providers-fab>=2.4.0

# ── Dépendances applicatives ──────────────────────────────────────────────────
PyYAML>=6.0.2
python-dotenv>=1.1.1
psycopg2-binary>=2.9.10
PyMySQL>=1.1.1
# Jinja2 géré par apache-airflow-core (>=3.1.5 requis par airflow 3.2.1)
# Ne pas pinner séparément pour éviter les conflits.

# ── Dev / tests ───────────────────────────────────────────────────────────────
pytest>=8.4.1
